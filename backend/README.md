# AI PR Reviewer - Backend

基于 FastAPI 的代码智能评审系统后端服务。

## 快速启动

1. 激活虚拟环境：
   
   ```
   .venv\Scripts\Activate.ps1
   ```

2. 安装依赖：

   ```
   pip install -r requirements.txt
   ```

3. 启动开发服务器：

   ```
   python -m app.main
   ```

   